[pytest]
# Keep test discovery narrow and deterministic for CI stability.
testpaths = tests
addopts = -ra --strict-config --strict-markers
norecursedirs = test-scripts
xfail_strict = true
filterwarnings =
    # Treat warnings as failures to prevent quality regressions.
    error
markers =
    ha_integration: test requiring Home Assistant integration harness behavior
